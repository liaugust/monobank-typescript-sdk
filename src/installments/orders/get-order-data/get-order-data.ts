/** Root-relative endpoint reading the settlement details of an order. */
export const getInstallmentsOrderDataEndpoint = "/api/order/data";

/** Root-relative endpoint reading the settlement details of an order. */
export const getInstallmentsOrderInfoEndpoint = "/api/order/info";
